const c=document.querySelector('#c'),x=c.getContext('2d');let W,H,run=false,diff=1,wave=1,score=0,hp=100,mana=100,keys={},shots=[],foes=[],parts=[],px=0,py=0,aim=-Math.PI/2,last=0,spawn=0,kills=0,need=8;const $=s=>document.querySelector(s);function resize(){W=c.width=innerWidth*devicePixelRatio;H=c.height=(innerHeight-88)*devicePixelRatio;x.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);W/=devicePixelRatio;H/=devicePixelRatio;if(!run){px=W/2;py=H*.68}}addEventListener('resize',resize);resize();document.querySelectorAll('[data-d]').forEach(b=>b.onclick=()=>{document.querySelectorAll('[data-d]').forEach(q=>q.classList.remove('sel'));b.classList.add('sel');diff=+b.dataset.d});$('#start').onclick=()=>{run=true;$('#menu').hidden=true;['hud','stats','powers','touch'].forEach(id=>$('#'+id).hidden=false);resetWave()};function resetWave(){foes=[];shots=[];kills=0;need=7+wave*2;spawn=0;hp=Math.min(100,hp+25);mana=100;$('#wave').textContent=String(wave).padStart(2,'0')+' / 12';$('#target').textContent=wave%4===0?'WAR MACHINE // BOSS':'INVASION FORCE';update()}function update(){$('#hp').textContent=Math.max(0,hp|0);$('#mana').textContent=mana|0;$('#score').textContent=String(score).padStart(6,'0');$('#hpbar').style.width=hp+'%';$('#manabar').style.width=mana+'%';$('#bossbar').style.width=Math.max(0,100-kills/need*100)+'%'}addEventListener('keydown',e=>{keys[e.key.toLowerCase()]=1;if(e.code==='Space'){e.preventDefault();fire()}if('123'.includes(e.key))power(+e.key)});addEventListener('keyup',e=>keys[e.key.toLowerCase()]=0);c.addEventListener('pointermove',e=>{if(run&&e.buttons)aim=Math.atan2(e.offsetY-py,e.offsetX-px)});c.addEventListener('pointerdown',e=>{if(run){aim=Math.atan2(e.offsetY-py,e.offsetX-px);fire()}});$('#fire').onpointerdown=e=>{e.preventDefault();fire()};document.querySelectorAll('[data-p]').forEach((b,i)=>b.onclick=()=>power(i+1));function fire(){if(!run)return;shots.push({x:px,y:py,vx:Math.cos(aim)*720,vy:Math.sin(aim)*720,r:4,life:1.2,p:18})}function power(n){if(!run)return;if(n===1&&mana>=8){mana-=8;for(let a=-.14;a<=.14;a+=.14)shots.push({x:px,y:py,vx:Math.cos(aim+a)*620,vy:Math.sin(aim+a)*620,r:6,life:1.1,p:22})}if(n===2&&mana>=25){mana-=25;hp=Math.min(100,hp+24)}if(n===3&&mana>=45){mana-=45;foes.forEach(f=>f.hp-=55);boom(px,py,30)}update()}function enemy(){let side=Math.random()*4|0,X=side%2?Math.random()*W:side?W:-20,Y=side%2?(side===1?-20:H):Math.random()*H;let boss=wave%4===0&&kills===need-1;foes.push({x:X,y:Y,r:boss?30:14,hp:boss?150:32+wave*3,sp:(55+wave*4)*(1+diff*.18),boss,hit:0})}function boom(X,Y,n=12){for(let i=0;i<n;i++){let a=Math.random()*6.28,s=Math.random()*160;parts.push({x:X,y:Y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,l:1})}}function end(win){run=false;$('#dt').textContent=win?'LATVERIA STANDS':'ARMOR BREACHED';$('#dp').textContent=win?'The citadel remains under your command. Final score: '+score+'.':'The invasion overwhelmed your defenses on wave '+wave+'.';$('#dialog').showModal();$('#next').textContent=win?'RETURN TO BRIEFING':'RETRY WAVE →'}$('#next').onclick=()=>{$('#dialog').close();if(wave>12){location.reload();return}run=true;if(hp<=0)hp=100;resetWave()};let joy=$('#joy'),stick=joy.querySelector('i'),jdx=0,jdy=0;joy.onpointerdown=e=>{joy.setPointerCapture(e.pointerId)};joy.onpointermove=e=>{if(!joy.hasPointerCapture(e.pointerId))return;let r=joy.getBoundingClientRect(),dx=e.clientX-(r.left+r.width/2),dy=e.clientY-(r.top+r.height/2),m=Math.max(1,Math.hypot(dx,dy)),q=Math.min(25,m);jdx=dx/m*q/25;jdy=dy/m*q/25;stick.style.transform=`translate(${jdx*22}px,${jdy*22}px)`};joy.onpointerup=()=>{jdx=jdy=0;stick.style.transform=''};function loop(t){let dt=Math.min(.03,(t-last)/1000||0);last=t;draw(dt);requestAnimationFrame(loop)}function draw(dt){let g=x.createLinearGradient(0,0,0,H);g.addColorStop(0,'#101d17');g.addColorStop(1,'#020604');x.fillStyle=g;x.fillRect(0,0,W,H);x.fillStyle='#15251c';for(let i=0;i<9;i++){let bx=i*W/8-30,bh=80+(i%3)*55;x.fillRect(bx,H*.36-bh,W/7,bh);x.fillStyle='#24432f';for(let j=0;j<3;j++)x.fillRect(bx+15+j*25,H*.36-bh+20,8,18);x.fillStyle='#15251c'}x.fillStyle='#0b120e';x.fillRect(0,H*.36,W,H);x.strokeStyle='#203a2a';for(let y=H*.45;y<H;y+=55){x.beginPath();x.moveTo(0,y);x.lineTo(W,y);x.stroke()}if(run){let sp=210*dt,dx=(keys.d?1:0)-(keys.a?1:0)+jdx,dy=(keys.s?1:0)-(keys.w?1:0)+jdy;px=Math.max(20,Math.min(W-20,px+dx*sp));py=Math.max(H*.38,Math.min(H-20,py+dy*sp));mana=Math.min(100,mana+7*dt);spawn-=dt;if(spawn<=0&&kills+foes.length<need){enemy();spawn=Math.max(.25,1.15-wave*.045-diff*.12)}shots.forEach(s=>{s.x+=s.vx*dt;s.y+=s.vy*dt;s.life-=dt});shots=shots.filter(s=>s.life>0);foes.forEach(f=>{let a=Math.atan2(py-f.y,px-f.x),ds=Math.hypot(px-f.x,py-f.y);f.x+=Math.cos(a)*f.sp*dt;f.y+=Math.sin(a)*f.sp*dt;f.hit-=dt;if(ds<f.r+22&&f.hit<=0){hp-=f.boss?18:8+diff*3;f.hit=.7;boom(px,py,6)}shots.forEach(s=>{if(s.life>0&&Math.hypot(s.x-f.x,s.y-f.y)<f.r+s.r){f.hp-=s.p;s.life=0;boom(s.x,s.y,4)}})});let dead=foes.filter(f=>f.hp<=0);dead.forEach(f=>{kills++;score+=f.boss?1500:100+wave*15;boom(f.x,f.y,f.boss?40:15)});foes=foes.filter(f=>f.hp>0);if(hp<=0){hp=0;update();end(false)}else if(kills>=need){wave++;score+=500;if(wave>12)end(true);else{run=false;$('#dt').textContent='WAVE CLEARED';$('#dp').textContent='The invaders retreat deeper into the capital. Armor systems are recharging.';$('#next').textContent='BEGIN WAVE '+wave+' →';$('#dialog').showModal()}}update()}foes.forEach(f=>{x.save();x.translate(f.x,f.y);x.fillStyle=f.boss?'#a5ffb7':'#75857b';x.shadowColor='#72ff91';x.shadowBlur=f.boss?25:5;x.fillRect(-f.r*.7,-f.r,f.r*1.4,f.r*2);x.fillStyle='#ff3d3d';x.fillRect(-f.r*.45,-f.r*.45,f.r*.9,3);x.restore()});shots.forEach(s=>{x.fillStyle='#8dffa3';x.shadowColor='#63ff82';x.shadowBlur=15;x.beginPath();x.arc(s.x,s.y,s.r,0,7);x.fill();x.shadowBlur=0});parts.forEach(p=>{p.x+=p.vx*dt;p.y+=p.vy*dt;p.l-=dt;x.globalAlpha=Math.max(0,p.l);x.fillStyle='#7cff94';x.fillRect(p.x,p.y,3,3)});x.globalAlpha=1;parts=parts.filter(p=>p.l>0);x.save();x.translate(px,py);x.rotate(aim);x.fillStyle='#173522';x.strokeStyle='#86ffa0';x.lineWidth=2;x.beginPath();x.arc(0,0,18,0,7);x.fill();x.stroke();x.fillStyle='#9dffae';x.fillRect(12,-5,24,10);x.restore();x.strokeStyle='#aaffba88';x.beginPath();x.arc(px+Math.cos(aim)*55,py+Math.sin(aim)*55,8,0,7);x.stroke()}requestAnimationFrame(loop);